import { BestpraticePipe } from './bestpratice.pipe';

describe('BestpraticePipe', () => {
  it('create an instance', () => {
    const pipe = new BestpraticePipe();
    expect(pipe).toBeTruthy();
  });
});
