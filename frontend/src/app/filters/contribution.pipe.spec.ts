import { ContributionPipe } from './contribution.pipe';

describe('ContributionPipe', () => {
  it('create an instance', () => {
    const pipe = new ContributionPipe();
    expect(pipe).toBeTruthy();
  });
});
