import { SanctionorderPipe } from './sanctionorder.pipe';

describe('SanctionorderPipe', () => {
  it('create an instance', () => {
    const pipe = new SanctionorderPipe();
    expect(pipe).toBeTruthy();
  });
});
