import { RemoveArrayPipe } from './remove-array.pipe';

describe('RemoveArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
