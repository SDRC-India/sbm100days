import { RemoveTableColumPipe } from './remove-table-colum.pipe';

describe('RemoveTableColumPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveTableColumPipe();
    expect(pipe).toBeTruthy();
  });
});
